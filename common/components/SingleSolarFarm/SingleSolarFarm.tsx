import { Grid, Typography } from "@mui/material";

export const SingleSolarFarm = () => {
  return (
    <Grid sx={{ p: "4rem" }}>
      <Typography variant="h1">Farm Name</Typography>
    </Grid>
  );
};
